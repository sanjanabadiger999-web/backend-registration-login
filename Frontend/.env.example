# Copy this file to `.env` for local development and set the values.
# In Vercel, set these as Environment Variables instead (never in code).

# AuthenticationService (login, /api/me, /api/logout) - Render
VITE_API_URL=https://reglogin-auth-service.onrender.com

# UserService (registration /api/reg) - Render
VITE_REGISTER_URL=https://reglogin-user-service.onrender.com