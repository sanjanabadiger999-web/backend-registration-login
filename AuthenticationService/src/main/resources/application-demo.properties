# ============================================================================
# DEMO PROFILE - runs without MySQL (uses the same shared file-based H2 DB as
# UserService). Activate with:  --spring.profiles.active=demo
# The default profile (application.properties) still uses MySQL on localhost:3306.
# ============================================================================
server.port=8082

# JWT secret used for LOCAL demo ONLY. Production MUST set the JWT_SECRET
# environment variable (the default profile requires it - no fallback).
jwt.secret=${JWT_SECRET:MDEyMzQ1Njc4OWFiY2RlZjAxMjM0NTY3ODlhYmNkZWY=}

spring.datasource.url=jdbc:h2:file:C:/Users/SANJAN~1/AppData/Local/Temp/opencode/reglogin/reglogin;AUTO_SERVER=TRUE;MODE=MySQL;DATABASE_TO_LOWER=TRUE
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver

spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.globally_quoted_identifiers=true